# Server A DB container name must be resolvable from the Docker network.
SOURCE_DATABASE_URL=postgresql://postgres:change-me@odt-pipeline-db:5432/vectordb

# Replace with Server B's actual address and credential. Do not commit sync.env.
TARGET_DATABASE_URL=postgresql://cmyoon:change-me@<SERVER_B_IP>:5432/vectordb
